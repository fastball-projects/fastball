package dev.fastball.components.list.config;

public enum DataListFieldPosition {
    /**
     * 卡片标题, 一般推荐只有一个字段
     */
    Title,

    /**
     * 卡片内容字段
     */
    Content,

    /**
     * 卡片状态字段, 默认会显示在标题右侧
     */
    Status,
}
